{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the app's own windows, and nothing else.",
  "windows": ["rec-bar", "detail"],
  "permissions": [
    "core:default"
  ]
}
